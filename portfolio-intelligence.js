(function(root,factory){const api=factory();if(typeof module!=='undefined'&&module.exports)module.exports=api;if(root)root.PortfolioIntelligence=api;})(typeof window!=='undefined'?window:globalThis,function(){
  const n=v=>Number.isFinite(Number(v))?Number(v):0;
  function classify(a){const t=String(a?.type||'').toLowerCase();if(t.includes('renda fixa'))return 'FIXED_INCOME';if(t.includes('fii'))return 'FII';if(t.includes('ação')||t.includes('acao'))return 'STOCK';if(t.includes('etf'))return 'ETF';return 'OTHER';}
  function buildAssetMetrics(assets,quotes){return (Array.isArray(assets)?assets:[]).map(a=>{const q=quotes[String(a.ticker||'').toUpperCase()];const qty=n(a.qty||a.quantity);const invested=qty*n(a.pm||a.average_price||a.avg_price);const price=q?.price==null?null:n(q.price);const value=price==null?null:qty*price;const gain=value==null?null:value-invested;return {ticker:String(a.ticker||''),assetType:classify(a),quantity:qty,averagePrice:n(a.pm||a.average_price||a.avg_price),invested,marketPrice:price,marketValue:value,capitalGainLoss:gain,capitalReturnPercent:invested?gain/invested*100:null,quoteTimestamp:q?.timestamp||null,quoteProvider:q?.provider||null,quoteStatus:q?.ok?'FRESH':'UNAVAILABLE'};});}
  function summarize(assets,quotes){const rows=buildAssetMetrics(assets,quotes);const priced=rows.filter(r=>r.marketValue!=null);const invested=rows.reduce((s,r)=>s+r.invested,0);const marketValue=priced.reduce((s,r)=>s+r.marketValue,0);const gain=priced.length===rows.length?marketValue-invested:null;return {rows,pricedCount:priced.length,assetCount:rows.length,priceCoveragePercent:rows.length?priced.length/rows.length*100:0,totalInvested:invested,currentMarketValue:marketValue,capitalGainLoss:gain,capitalReturnPercent:gain==null||!invested?null:gain/invested*100};}
  return {classify,buildAssetMetrics,summarize};
});
