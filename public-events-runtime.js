/* Safe public-event lifecycle: cache-first, background refresh, no financial/cloud writes. */
(function(root,factory){const api=factory(root);if(typeof module!=='undefined'&&module.exports)module.exports=api;if(root)root.PublicEventsRuntime=api;})(typeof window!=='undefined'?window:globalThis,function(root){
  function create({store,storage,provider,assets=[],transactions=[],now=()=>Date.now(),ttlMs=60*60*1000,onUpdate=()=>{}}={}){let timer=null,started=false;const read=()=>store.read(storage);const publish=(events,status,updatedAt)=>{root.__CORPORATE_EVENTS_RUNTIME__={events,status,updatedAt,source:'public-read-only'};onUpdate(root.__CORPORATE_EVENTS_RUNTIME__);};const refresh=async()=>{const result=await provider.syncPortfolio({assets,transactions});const current=read();if(result?.events){const saved=store.write(storage,{events:result.events,quotes:current.quotes});publish(saved.events,result.status||'UPDATED',saved.updatedAt);return {ok:true,...result};}publish(current.events,'PARTIAL',current.updatedAt);return {ok:false,status:'PARTIAL',events:current.events};};const start=async()=>{if(started)return;started=true;const current=read();publish(current.events,store.isFresh(current.updatedAt,ttlMs,now())?'CACHED':'UPDATING',current.updatedAt);await refresh();timer=setInterval(()=>{refresh().catch(()=>publish(read().events,'OFFLINE',read().updatedAt));},ttlMs);return root.__CORPORATE_EVENTS_RUNTIME__;};const stop=()=>{if(timer)clearInterval(timer);timer=null;started=false;};return {start,refresh,stop,read};}
  return {create};
});
